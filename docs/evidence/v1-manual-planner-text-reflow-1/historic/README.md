# Historic first-pass captures — not matched baseline proof

These files are the first author-session captures from task-seed / dirty trees.
They are kept for audit trail only.

- `before_before_text-200_390x844_budget.png` is **not** budget proof. It shows the idea form (scrollY ~1373; budget field was around y=3306). Source was `84deca89` with a dirty audit script, not clean `4278cd047b907b218fe64c122c4eed7dd61e0a7e`.
- Geometry for that first residual remains in `audit-before.json` here, but RF-R2 matched visible 390 budget-before and 360 pointer-before live under `screens/before_*` from unmodified baseline `4278cd` compiled CSS.

Do not treat this folder as the RF-R2 matched pair.
