# V1 Workspace Usability 1 — Adversarial self-review

Stand: 21. September 2026  
Status: **AGENT SELF-REVIEW AFTER INTEGRATION + R2 REBIND — NOT A TECHNICAL-LEAD PASS**

---

## 1. Finding resolution

### VUX-R1

Unchanged source fix. Calendar round-trip. Not re-litigated here.

### VUX-R2

Unchanged source fix. Narrow R2 recapture on **clean** `dda36b17` (integrated `66af1539` + harness metadata). Manual scroll 1038 survived booking-toggle parent update and explicit search (jump 0). No reset to open-snap 433.

### Rapid close

Previous STATUS over-claimed “before deferred work”. The script waits for detail mount and focuses the in-detail back control before Escape. That is **return evidence**, not a measurement that rAF/timeout were still queued. Relabelled in harness and `vux-r2-interaction.json`.

### Evidence binding

The earlier R2 JSON named parent `4b5f34f9` while the working tree was dirty. That mismatch is closed: recapture records `productTree.head = dda36b17`, `workingTree = clean`, `dirtyPaths = []`.

---

## 2. Remaining risks

- First compositor frame of compact open is still not proven for every GPU.
- Rapid-close screenshot can land mid-overview (`scrollY` 565) with the Übersicht heading at ~1.5px; that is useful return context, not exact pre-open Y restoration.
- ICU month abbreviations remain Node-bound.
- #517 guest-storage files arrived via merge only; this writer did not review them as owner.

---

## 3. Scope held

No homepage/hero, VUX-3/VUX-5, sibling merge, Ready, or PR merge. Shared-path conflict: none.

---

## 4. Verdict

Author self-review: integration and evidence binding match the TL boundary instruction. **Not TL PASS.**
