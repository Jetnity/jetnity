# ─────────────────────────────────────────────────────────
# Jetnity – Beispiel-Umgebungsvariablen (.env.example)
# Werte hier sind Platzhalter. Niemals echte Secrets committen.
# ─────────────────────────────────────────────────────────

# Öffentlich (Client darf sie lesen)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# OpenAI (Server liest das; auf Vercel als Secret setzen)
OPENAI_API_KEY=your-openai-api-key

# Optionale Flags / Dev-Helpers
NEXT_PUBLIC_RENDER_SIMULATE=0
RENDER_SIMULATE=0

# Optional: Für strukturierte Daten / kanonische URLs
NEXT_PUBLIC_SITE_URL=https://jetnity.com

# Server-Only Secrets (nicht lokal ins Repo!)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# CRON_SECRET=your-very-long-random-token
